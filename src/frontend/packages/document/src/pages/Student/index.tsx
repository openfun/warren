/**
 * A React component responsible for rendering a dashboard of document statistics
 * for student.
 *
 *
 * @returns {JSX.Element} The JSX for the document statistics overview dashboard.
 */
export default () => {
  return (
    <div className="c__overview">
      No student view available for document indicators
    </div>
  );
};
